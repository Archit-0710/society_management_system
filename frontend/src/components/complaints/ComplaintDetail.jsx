import { PriorityBadge, StatusBadge } from '../common/Badges'

export function ComplaintDetail({ complaint }) { return <><section className="detail-grid"><article><span>Category</span><strong>{complaint.category_name}</strong></article><article><span>Status</span><StatusBadge status={complaint.status} /></article><article><span>Priority</span><PriorityBadge priority={complaint.priority} /></article><article><span>Created</span><strong>{new Date(complaint.created_at).toLocaleString()}</strong></article>{complaint.resolved_at && <article><span>Resolved</span><strong>{new Date(complaint.resolved_at).toLocaleString()}</strong></article>}</section><section className="content-section"><h2>Description</h2><p>{complaint.description}</p>{complaint.photo_url && <img className="complaint-photo" src={complaint.photo_url} alt={`Complaint ${complaint.id}`} />}</section><section className="content-section"><h2>Status history</h2><ol className="timeline">{complaint.status_history?.map((item) => <li key={item.id}><StatusBadge status={item.new_status} /><div><strong>{item.old_status ? `${item.old_status.replace('_', ' ')} to ` : ''}{item.new_status.replace('_', ' ')}</strong><p>{item.note || 'No note provided.'}</p><time>{new Date(item.created_at).toLocaleString()}</time></div></li>)}</ol></section></> }
