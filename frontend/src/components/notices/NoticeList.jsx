export function NoticeList({ notices }) { return <div className="notice-list">{notices.map((notice) => <article className={`notice ${notice.is_important ? 'important' : ''}`} key={notice.id}><div className="notice-meta">{notice.is_important && <span className="badge priority-high">Important</span>}<time>{new Date(notice.created_at).toLocaleDateString()}</time></div><h2>{notice.title}</h2><p>{notice.content}</p></article>)}</div> }
